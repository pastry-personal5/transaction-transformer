import os


class Constants:
    """A class to hold constant values for the application."""

    input_data_dir_path = os.path.join(".", "data", "active")
    output_data_dir_path = os.path.join(".", "output")
    config_dir_path = os.path.join(".", "config", "active")
